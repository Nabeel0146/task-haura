import 'package:flutter/material.dart';

class Productivitypage extends StatelessWidget {
  const Productivitypage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Priority Page', style: TextStyle(fontSize: 24)),
    );
  }
}